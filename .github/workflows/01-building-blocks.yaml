name: 01 - Building Blocks
# comment
on: workflow_dispatch

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Say hello
              run: echo "Hello,World!"

    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Succes step
              run: |
               echo "I will Success"
               exit 0
            - name: Say Goodbye
              run: echo "Goodbye"
